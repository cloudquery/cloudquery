variables:
  component: destination/kinesisfirehose
  binary: kinesisfirehose

project_name: plugins/destination/kinesisfirehose

monorepo:
  tag_prefix: plugins-destination-kinesisfirehose-
  dir: plugins/destination/kinesisfirehose

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml