variables:
  component: destination/mongodb
  binary: mongodb

project_name: plugins/destination/mongodb

monorepo:
  tag_prefix: plugins-destination-mongodb-
  dir: plugins/destination/mongodb

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml