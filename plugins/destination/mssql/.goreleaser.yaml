variables:
  component: destination/mssql
  binary: mssql

project_name: plugins/destination/mssql

monorepo:
  tag_prefix: plugins-destination-mssql-
  dir: plugins/destination/mssql

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml