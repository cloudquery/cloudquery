variables:
  component: destination/gremlin
  binary: gremlin

project_name: plugins/destination/gremlin

monorepo:
  tag_prefix: plugins-destination-gremlin-
  dir: plugins/destination/gremlin

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml