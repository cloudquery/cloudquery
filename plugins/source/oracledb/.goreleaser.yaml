variables:
  component: source/oracledb
  binary: oracledb

project_name: plugins/source/oracledb

monorepo:
  tag_prefix: plugins-source-oracledb-
  dir: plugins/source/oracledb

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml