variables:
  component: source/mixpanel
  binary: mixpanel

project_name: plugins/source/mixpanel

monorepo:
  tag_prefix: plugins-source-mixpanel-
  dir: plugins/source/mixpanel

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml