variables:
  component: source/salesforce
  binary: salesforce

project_name: plugins/source/salesforce

monorepo:
  tag_prefix: plugins-source-salesforce-
  dir: plugins/source/salesforce

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml