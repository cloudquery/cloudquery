// Code generated by codegen; DO NOT EDIT.

package cloudfunctions

import (
	"context"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"testing"

	faker "github.com/cloudquery/faker/v3"
	"github.com/cloudquery/plugins/source/gcp/client"
	"github.com/julienschmidt/httprouter"

	"google.golang.org/api/cloudfunctions/v1"

	"google.golang.org/api/option"
)

type MockFunctionsResult struct {
	Functions []*cloudfunctions.CloudFunction `json:"functions,omitempty"`
}

func createFunctions() (*client.Services, error) {
	var item cloudfunctions.CloudFunction
	if err := faker.FakeData(&item); err != nil {
		return nil, err
	}

	mux := httprouter.New()
	mux.GET("/*filepath", func(w http.ResponseWriter, r *http.Request, _ httprouter.Params) {
		resp := &MockFunctionsResult{
			Functions: []*cloudfunctions.CloudFunction{&item},
		}
		b, err := json.Marshal(resp)
		if err != nil {
			http.Error(w, "unable to marshal request: "+err.Error(), http.StatusBadRequest)
			return
		}
		if _, err := w.Write(b); err != nil {
			http.Error(w, "failed to write", http.StatusBadRequest)
			return
		}
	})
	ts := httptest.NewServer(mux)
	svc, err := cloudfunctions.NewService(context.Background(), option.WithoutAuthentication(), option.WithEndpoint(ts.URL))
	if err != nil {
		return nil, err
	}
	return &client.Services{
		Cloudfunctions: svc,
	}, nil
}

func TestFunctions(t *testing.T) {
	client.MockTestHelper(t, Functions(), createFunctions, client.TestOptions{})
}
