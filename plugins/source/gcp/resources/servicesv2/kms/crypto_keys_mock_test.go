// Code generated by codegen; DO NOT EDIT.

package kms

import (
	"context"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"testing"

	faker "github.com/cloudquery/faker/v3"
	"github.com/cloudquery/plugins/source/gcp/client"
	"github.com/julienschmidt/httprouter"

	"google.golang.org/api/cloudkms/v1"

	"google.golang.org/api/option"
)

type MockCryptoKeysResult struct {
	Items []*kms.CryptoKey `json:"items,omitempty"`
}

func createCryptoKeys() (*client.Services, error) {
	var item kms.CryptoKey
	if err := faker.FakeData(&item); err != nil {
		return nil, err
	}

	mux := httprouter.New()
	mux.GET("/*filepath", func(w http.ResponseWriter, r *http.Request, _ httprouter.Params) {
		resp := &MockCryptoKeysResult{
			Items: []*kms.CryptoKey{&item},
		}
		b, err := json.Marshal(resp)
		if err != nil {
			http.Error(w, "unable to marshal request: "+err.Error(), http.StatusBadRequest)
			return
		}
		if _, err := w.Write(b); err != nil {
			http.Error(w, "failed to write", http.StatusBadRequest)
			return
		}
	})
	ts := httptest.NewServer(mux)
	svc, err := kms.NewService(context.Background(), option.WithoutAuthentication(), option.WithEndpoint(ts.URL))
	if err != nil {
		return nil, err
	}
	return &client.Services{
		Kms: svc,
	}, nil
}

func TestCryptoKeys(t *testing.T) {
	client.MockTestHelper(t, CryptoKeys(), createCryptoKeys, client.TestOptions{})
}
