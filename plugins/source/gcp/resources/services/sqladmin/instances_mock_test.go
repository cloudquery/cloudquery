// Code generated by codegen; DO NOT EDIT.

package sqladmin

import (
	"context"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/bxcodec/faker/v4"
	"github.com/cloudquery/plugins/source/gcp/client"
	"github.com/julienschmidt/httprouter"
	
  "google.golang.org/api/sqladmin/v1beta4"
  
	"google.golang.org/api/option"
)

type MockInstancesResult struct {
   []*sqladmin.DatabaseInstance `json:",omitempty"`
}

func createInstances() (*client.Services, error) {
	var item sqladmin.DatabaseInstance
	if err := faker.FakeData(&item); err != nil {
		return nil, err
	}
  
	mux := httprouter.New()
	mux.GET("/*filepath", func(w http.ResponseWriter, r *http.Request, _ httprouter.Params) {
		resp := &MockInstancesResult{
			: []*sqladmin.DatabaseInstance{&item},
		}
		b, err := json.Marshal(resp)
		if err != nil {
			http.Error(w, "unable to marshal request: "+err.Error(), http.StatusBadRequest)
			return
		}
		if _, err := w.Write(b); err != nil {
			http.Error(w, "failed to write", http.StatusBadRequest)
			return
		}
	})
	ts := httptest.NewServer(mux)
	svc, err := sqladmin.NewService(context.Background(), option.WithoutAuthentication(), option.WithEndpoint(ts.URL))
	if err != nil {
		return nil, err
	}
	return &client.Services{
		Sqladmin: svc,
	}, nil
}

func TestInstances(t *testing.T) {
	client.MockTestHelper(t, Instances(), createInstances, client.TestOptions{})
}