// Code generated by codegen; DO NOT EDIT.

package logging

import (
	"context"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/bxcodec/faker/v4"
	"github.com/cloudquery/plugins/source/gcp/client"
	"github.com/julienschmidt/httprouter"

	"google.golang.org/api/logging/v2"

	"google.golang.org/api/option"
)

type MockSinksResult struct {
	Sinks []*logging.LogSink `json:"sinks,omitempty"`
}

func createSinks() (*client.Services, error) {
	var item logging.LogSink
	if err := faker.FakeData(&item); err != nil {
		return nil, err
	}

	mux := httprouter.New()
	mux.GET("/*filepath", func(w http.ResponseWriter, r *http.Request, _ httprouter.Params) {
		resp := &MockSinksResult{
			Sinks: []*logging.LogSink{&item},
		}
		b, err := json.Marshal(resp)
		if err != nil {
			http.Error(w, "unable to marshal request: "+err.Error(), http.StatusBadRequest)
			return
		}
		if _, err := w.Write(b); err != nil {
			http.Error(w, "failed to write", http.StatusBadRequest)
			return
		}
	})
	ts := httptest.NewServer(mux)
	svc, err := logging.NewService(context.Background(), option.WithoutAuthentication(), option.WithEndpoint(ts.URL))
	if err != nil {
		return nil, err
	}
	return &client.Services{
		Logging: svc,
	}, nil
}

func TestSinks(t *testing.T) {
	client.MockTestHelper(t, Sinks(), createSinks, client.TestOptions{})
}
