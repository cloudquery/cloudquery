variables:
  component: source/googleads
  binary: googleads

project_name: plugins/source/googleads

monorepo:
  tag_prefix: plugins-source-googleads-
  dir: plugins/source/googleads

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml