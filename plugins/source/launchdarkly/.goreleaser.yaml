variables:
  component: source/launchdarkly
  binary: launchdarkly

project_name: plugins/source/launchdarkly

monorepo:
  tag_prefix: plugins-source-launchdarkly-
  dir: plugins/source/launchdarkly

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml