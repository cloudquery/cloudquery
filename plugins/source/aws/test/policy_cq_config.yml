kind: source
spec:
  name: aws
  version: v1.0.0
  destinations: [postgresql]
  path: ./aws
  registry: local
---
kind: destination
spec:
  name: postgresql
  version: "v1.6.5" # latest version of postgresql plugin
  spec:
    connection_string: ${CQ_DSN}