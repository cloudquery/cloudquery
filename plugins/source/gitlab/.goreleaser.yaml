variables:
  component: source/gitlab
  binary: gitlab

project_name: plugins/source/gitlab

monorepo:
  tag_prefix: plugins-source-gitlab-
  dir: plugins/source/gitlab

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml