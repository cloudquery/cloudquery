variables:
  component: source/postgresql
  binary: postgresql

project_name: plugins/source/postgresql

monorepo:
  tag_prefix: plugins-source-postgresql-
  dir: plugins/source/postgresql

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml