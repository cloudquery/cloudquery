variables:
  component: source/shopify
  binary: shopify

project_name: plugins/source/shopify

monorepo:
  tag_prefix: plugins-source-shopify-
  dir: plugins/source/shopify

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml