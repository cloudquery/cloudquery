variables:
  component: source/vercel
  binary: vercel

project_name: plugins/source/vercel

monorepo:
  tag_prefix: plugins-source-vercel-
  dir: plugins/source/vercel

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml