variables:
  component: source/gandi
  binary: gandi

project_name: plugins/source/gandi

monorepo:
  tag_prefix: plugins-source-gandi-
  dir: plugins/source/gandi

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml