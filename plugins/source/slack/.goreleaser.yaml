variables:
  component: source/slack
  binary: slack

project_name: plugins/source/slack

monorepo:
  tag_prefix: plugins-source-slack-
  dir: plugins/source/slack

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml
