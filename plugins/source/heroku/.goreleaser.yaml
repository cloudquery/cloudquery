variables:
  component: source/heroku

project_name: plugins/source/heroku

monorepo:
  tag_prefix: plugins-source-heroku-
  dir: plugins/source/heroku

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml