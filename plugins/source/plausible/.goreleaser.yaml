variables:
  component: source/plausible
  binary: plausible

project_name: plugins/source/plausible

monorepo:
  tag_prefix: plugins-source-plausible-
  dir: plugins/source/plausible

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml