cloudquery:
  connection:
    dsn: "postgres://postgres:pass@localhost:5432/postgres"

  providers:
    - name: test
      source: cloudquery
      version: "v0.0.0"

providers:
  aws:
    alias: "same-aws"
    configuration:
      accounts:
        dev:
          role_arn: "12312312"
        ron:
    resources: ["slow_resource"]
  aws-2:
    alias: "same-aws"
    configuration:
      accounts:
        dev:
          role_arn: "12312312"
        ron:
    resources: ["slow_resource"]

