name: Destination Plugin Elasticsearch Workflow

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - "plugins/destination/elasticsearch/**"
      - ".github/workflows/dest_elasticsearch.yml"
  push:
    branches:
      - main
    paths:
      - "plugins/destination/elasticsearch/**"
      - ".github/workflows/dest_elasticsearch.yml"

permissions:
  contents: read

jobs:
  plugins-destination-elasticsearch:
    timeout-minutes: 10
    name: "plugins/destination/elasticsearch"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./plugins/destination/elasticsearch
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2
      - name: Set up Go 1.x
        id: setup-go
        uses: actions/setup-go@v6
        with:
          go-version-file: plugins/destination/elasticsearch/go.mod
          cache: false
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ steps.setup-go.outputs.go-version }}-test-destination-elasticsearch-${{ hashFiles('plugins/destination/elasticsearch/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-${{ steps.setup-go.outputs.go-version }}-test-destination-elasticsearch-
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.10.1
          working-directory: plugins/destination/elasticsearch
          args: "--config ../../.golangci.yml"
      - name: gen
        if: github.event_name == 'pull_request'
        run: make gen
      - name: Fail if generation updated files
        if: github.event_name == 'pull_request'
        run: test "$(git status -s | wc -l)" -eq 0 || (git status -s; exit 1)
      - name: Build
        run: go build .
      - name: Test
        run: make test
