name: Destination Plugin Snowflake Workflow

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - "plugins/destination/snowflake/**"
      - ".github/workflows/dest_snowflake.yml"
  push:
    branches:
      - main
    paths:
      - "plugins/destination/snowflake/**"
      - ".github/workflows/dest_snowflake.yml"

permissions:
  contents: read

jobs:
  plugins-destination-snowflake:
    timeout-minutes: 30
    name: "plugins/destination/snowflake"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./plugins/destination/snowflake
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 2
      - name: Set up Go 1.x
        id: setup-go
        uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6
        with:
          go-version-file: plugins/destination/snowflake/go.mod
          cache: false
      - uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ steps.setup-go.outputs.go-version }}-test-destination-snowflake-${{ hashFiles('plugins/destination/snowflake/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-${{ steps.setup-go.outputs.go-version }}-test-destination-snowflake-
      - name: golangci-lint
        uses: golangci/golangci-lint-action@1e7e51e771db61008b38414a730f564565cf7c20 # v9
        with:
          version: v2.10.1
          working-directory: plugins/destination/snowflake
          args: "--config ../../.golangci.yml"
      - name: gen
        if: github.event_name == 'pull_request'
        run: make gen
      - name: Fail if generation updated files
        if: github.event_name == 'pull_request'
        run: test "$(git status -s | wc -l)" -eq 0 || (git status -s; exit 1)
      - name: Build
        run: go build .
      - name: Test
        run: make test
        env: # Or as an environment variable
          SNOW_TEST_DSN: ${{ secrets.SNOW_TEST_DSN }}
  