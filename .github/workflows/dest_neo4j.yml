name: Destination Plugin Neo4j Workflow

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - "plugins/destination/neo4j/**"
      - ".github/workflows/dest_neo4j.yml"
  push:
    branches:
      - main
    paths:
      - "plugins/destination/neo4j/**"
      - ".github/workflows/dest_neo4j.yml"

permissions:
  contents: read

jobs:
  plugins-destination-neo4j:
    timeout-minutes: 30
    name: "plugins/destination/neo4j"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./plugins/destination/neo4j
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2
      - name: Set up Go 1.x
        id: setup-go
        uses: actions/setup-go@v6
        with:
          go-version-file: plugins/destination/neo4j/go.mod
          cache: false
      - uses: actions/cache@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ steps.setup-go.outputs.go-version }}-test-destination-neo4j-${{ hashFiles('plugins/destination/neo4j/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-${{ steps.setup-go.outputs.go-version }}-test-destination-neo4j-
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.10.1
          working-directory: plugins/destination/neo4j
          args: "--config ../../.golangci.yml"
      - name: gen
        if: github.event_name == 'pull_request'
        run: make gen
      - name: Fail if generation updated files
        if: github.event_name == 'pull_request'
        run: test "$(git status -s | wc -l)" -eq 0 || (git status -s; exit 1)
      - name: Build
        run: go build .
      - name: Test Neo4j
        run: make test
