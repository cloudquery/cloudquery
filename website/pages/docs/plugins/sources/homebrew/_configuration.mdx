```yaml copy
kind: source
spec:
  name: "homebrew"
  path: "cloudquery/homebrew"
  version: "VERSION_SOURCE_HOMEBREW"
  tables: ["*"]
  destinations:
    - "DESTINATION_NAME"
  spec:
    backend: local
```
