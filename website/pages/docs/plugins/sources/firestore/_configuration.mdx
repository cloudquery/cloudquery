```yaml copy
kind: source
spec:
  # Source spec section
  name: firestore
  path: cloudquery/firestore
  version: "VERSION_SOURCE_FIRESTORE"
  # adjust this value up or down to change the number of requests that are made in parallel
  concurrency: 10000
  tables: ["*"]
  destinations: ["DESTINATION_NAME"]
  spec:
    # Firestore specific configuration goes here
```