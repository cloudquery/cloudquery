```yaml copy
kind: source
spec:
  # Source spec section
  name: aws
  path: cloudquery/aws
  version: "VERSION_SOURCE_AWS"
  tables: ["*"]
  destinations: ["DESTINATION_NAME"]
  spec:
    # AWS-specific configuration goes here
```