```yaml copy
kind: source
spec:
  name: "oracle"
  path: cloudquery/oracle
  version: "VERSION_SOURCE_ORACLE"
  destinations: ["DESTINATION_NAME"]
  # adjust this value up or down to change the number of tables that are resolved in parallel
  concurrency: 10000
  tables: ["*"]
  spec:
```