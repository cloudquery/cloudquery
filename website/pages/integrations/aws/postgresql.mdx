import Integration from "../../../components/pages/integration";
import {getPlugin} from "../../../components/pluginData";

<Integration source={getPlugin("aws")} destination={getPlugin("postgresql")}/>
