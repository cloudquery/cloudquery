import { Tabs, Tab } from '../Tabs'

<Tabs options={[ 'Precompiled Binaries (amd64)', 'Precompiled Binaries (arm64)' ]}>

<Tab>

```bash
curl -L https://github.com/cloudquery/cloudquery/releases/download/cli-vVERSION_CLI/cloudquery_linux_amd64 -o cloudquery
chmod a+x cloudquery
```

</Tab>

<Tab>

```bash
curl -L https://github.com/cloudquery/cloudquery/releases/download/cli-vVERSION_CLI/cloudquery_linux_arm64 -o cloudquery
chmod a+x cloudquery
```

</Tab>

</Tabs>