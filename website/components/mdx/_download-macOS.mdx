import { Tabs, Tab } from '../Tabs'

<Tabs options={['Homebrew', 'Precompiled Binaries (amd64)', 'Precompiled Binaries (arm64)']}>
<Tab>

```bash
brew install cloudquery/tap/cloudquery

# After initial install you can upgrade the version via:
# brew upgrade cloudquery

# To install a specific version use:
# brew install cloudquery/tap/cloudquery@VERSION_CLI
```

</Tab>

<Tab>

```bash
curl -L https://github.com/cloudquery/cloudquery/releases/download/cli-vVERSION_CLI/cloudquery_darwin_amd64 -o cloudquery
chmod a+x cloudquery
```

</Tab>

<Tab>

```bash
curl -L https://github.com/cloudquery/cloudquery/releases/download/cli-vVERSION_CLI/cloudquery_darwin_arm64 -o cloudquery
chmod a+x cloudquery
```

</Tab>

</Tabs>
