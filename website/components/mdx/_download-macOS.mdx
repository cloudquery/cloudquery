import { Tabs, Tab } from '../Tabs'

<Tabs options={['Homebrew', 'Precompiled Binaries (amd64)', 'Precompiled Binaries (arm64)']}>
<Tab>

```bash copy
brew install cloudquery/tap/cloudquery

# After initial install you can upgrade the version via:
# brew upgrade cloudquery
```

</Tab>

<Tab>

```bash copy
curl -L https://github.com/cloudquery/cloudquery/releases/download/VERSION_CLI/cloudquery_darwin_amd64 -o cloudquery
chmod a+x cloudquery
```

</Tab>

<Tab>

```bash copy
curl -L https://github.com/cloudquery/cloudquery/releases/download/VERSION_CLI/cloudquery_darwin_arm64 -o cloudquery
chmod a+x cloudquery
```

</Tab>

</Tabs>