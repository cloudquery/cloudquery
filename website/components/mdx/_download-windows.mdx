import { Tabs, Tab } from '../Tabs'

<Tabs options={['Precompiled Binaries (CMD)', 'Precompiled Binaries (PowerShell)']}>

<Tab>

```bash copy
curl -L https://github.com/cloudquery/cloudquery/releases/download/VERSION_CLI/cloudquery_windows_amd64.exe -o cloudquery.exe
```

</Tab>

<Tab>

```powershell copy
Invoke-WebRequest https://github.com/cloudquery/cloudquery/releases/download/VERSION_CLI/cloudquery_windows_amd64.exe -o cloudquery.exe
```

</Tab>

</Tabs>