package deploy

import (
	"testing"
)

const testConfig = `// Configuration AutoGenerated by CloudQuery CLI
cloudquery {
  plugin_directory = "."

  provider "gcp" {
    source  = "cloudquery/cq-provider-gcp"
    version = "v0.3.0-rc1"
  }

  connection {
    dsn = "host=localhost user=postgres password=pass DB.name=postgres port=5432"
  }
}
`

func TestTaskExecutor(t *testing.T) {
	// Skip for now because it create bugs
	//_, err := TaskExecutor(context.Background(), Request{
	//	TaskName: "fetch",
	//	Config:   []byte(testConfig),
	//})
	//assert.Nil(t, err)
}
