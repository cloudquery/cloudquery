package generate

import (
	"testing"
)

func TestGenerate(t *testing.T) {
	// dir, err := ioutil.TempDir(os.TempDir(), "test_generate")
	// if err != nil {
	// 	log.Fatal(err)
	// }
	// defer os.RemoveAll(dir)
	// sourceConfigPath := path.Join(dir, "source.cq.yaml")
	// cmd := NewCmdGenerate()
	// cmd.SetArgs([]string{"source", "aws", sourceConfigPath})
	// if err = cmd.Execute(); err != nil {
	// 	t.Fatal(err)
	// }
}
