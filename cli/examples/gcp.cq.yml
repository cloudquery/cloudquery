kind: source
spec:
  name: gcp
  version: v1.0.0
  registry: grpc
  path: "localhost:7777"
  max_goroutines: 10000000
  tables: ["*"]
  # skip_tables: ["gcp_compute_disk_types", "gcp_iam_service_accounts"]
  destinations:
    - postgresql
  